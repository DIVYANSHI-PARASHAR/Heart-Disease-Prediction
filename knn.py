# -*- coding: utf-8 -*-
"""heart_using_knn.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1yRTNwhz-Pj2l0ug_LUtJcZQZPITpIC_B
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
#DIVYANSHI PARASHAR
dataset= pd.read_csv('dataset.csv')

dataset.info()#DIVYANSHI PARASHAR

dataset.describe()
#DIVYANSHI PARASHAR

x = dataset.iloc[:, :-1].values
y = dataset.iloc[:, -1].values
#DIVYANSHI PARASHAR

from sklearn.model_selection import train_test_split
x_train, x_test, y_train, y_test = train_test_split(x, y, test_size = 0.25, random_state = 0)
#DIVYANSHI PARASHAR

print(x_train)
#DIVYANSHI PARASHAR

print(y_train)
#DIVYANSHI PARASHAR

print(x_test)
#DIVYANSHI PARASHAR

print(y_test)
#DIVYANSHI PARASHAR

from sklearn.preprocessing import StandardScaler
sc = StandardScaler()
x_train = sc.fit_transform(x_train)
x_test = sc.transform(x_test)
#DIVYANSHI PARASHAR

print(x_train)
#DIVYANSHI PARASHAR

print(x_test)
#DIVYANSHI PARASHAR

import seaborn as sns

corrmat = dataset.corr()
top_corr_features = corrmat.index
plt.figure(figsize=(20,20))

g=sns.heatmap(dataset[top_corr_features].corr(),annot=True,cmap="RdYlGn")
#DIVYANSHI PARASHAR

dataset.hist()
#DIVYANSHI PARASHAR

sns.set_style('whitegrid')
sns.countplot(x='target',data=dataset,palette='RdBu_r')
#DIVYANSHI PARASHAR

from sklearn.neighbors import KNeighborsClassifier
classifier = KNeighborsClassifier(n_neighbors = 8, metric = 'minkowski', p = 2)
classifier.fit(x_train, y_train)#DIVYANSHI PARASHAR

y_pred = classifier.predict(x_test)
print(np.concatenate((y_pred.reshape(len(y_pred),1), y_test.reshape(len(y_test),1)),1))
#DIVYANSHI PARASHAR

from sklearn.metrics import confusion_matrix, accuracy_score
cm = confusion_matrix(y_test, y_pred)
print(cm)
print('#DIVYANSHI PARASHAR')
accuracy_score(y_test, y_pred)
#DIVYANSHI PARASHAR

print(classifier.predict(sc.transform([[63,1,3,145,233,1,0,150,0,2.3,0,0,1]])))
#DIVYANSHI PARASHAR
